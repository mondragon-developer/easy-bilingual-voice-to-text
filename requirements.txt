# UI
customtkinter>=6.0.0

# Speech to text (offline; GPU-accelerated where available)
faster-whisper>=1.1.0

# Microphone capture
sounddevice>=0.5.0
numpy>=1.24

# EN <-> ES translation (Google, free, no API key; only part that needs internet)
deep-translator>=1.11.4

# System-wide hotkeys (Ctrl+Alt+R records even when the app is not focused).
# Not installed on macOS: hotkeys there need root, so the app skips them
# entirely (in-app shortcuts still work), and the package would pull in the
# whole pyobjc suite to support code that never runs there.
keyboard>=0.13.5; sys_platform != 'darwin'

# CUDA runtime libraries for NVIDIA GPU acceleration (Windows/Linux only;
# harmless on machines without an NVIDIA card - the app falls back to CPU).
nvidia-cublas-cu12; sys_platform != 'darwin'
nvidia-cudnn-cu12>=9; sys_platform != 'darwin'
